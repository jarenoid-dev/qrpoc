# Evidence — Step 003: Tablyx → code Code Connect mapping

Supporting evidence for [step 003](../../steps/2026-08-18-003-tablyx-code-connect-mapping.md).

- `code-connect-plan-blocker.txt` — the two Figma MCP API responses (with debug UUIDs)
  proving Code Connect is unavailable on the current plan/seat.
- `figma-components-inventory.md` — the 24 top-level Tablyx components with node ids and
  variant axes, measured live via the Figma Plugin API.
- `code-components-inventory.md` — the local-snapshot code components (46 shadcn/ui
  primitives + feature/screen components) with verified button/badge variant vocabularies.

Primary artifact (not in this directory): [`docs/code-connect-mapping.md`](../../../code-connect-mapping.md).

All evidence is textual and repository-safe. No product screenshots apply — this task is a
static code↔design mapping, not a UI change to a running product. No secrets included.
