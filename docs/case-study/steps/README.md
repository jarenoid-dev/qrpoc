# Step records

Create one Markdown record per decision-sized task by copying `../STEP-TEMPLATE.md`.

Name records `YYYY-MM-DD-NNN-short-slug.md`, starting with `001` for the first QRPOC product task after the evidence system is committed. Increase the sequence number across dates; do not reuse a number and do not add retroactive entries for earlier work.
